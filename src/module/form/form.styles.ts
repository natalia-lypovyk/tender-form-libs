import styled from 'styled-components';

const Form = styled.form`
  width: 70%;
  margin: 30px auto 0;
  padding: 10px;
  border: 1px solid var(--c-positive);
  border-radius: 6px;  
`;

export default { Form };
